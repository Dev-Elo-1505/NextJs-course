# Server vs Client components
Server components are components that run on the server not browser. Nextjs files are server components by default
Server components are best for fast initial page load, secure logic
Client components are useful for interactivity. they can hurt performance
To create client components, add "use client" at the top of the file

# Query Parameters
- use useParams for /shop/1/2